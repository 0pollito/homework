module.exports.select = function(){
  var connection = conectar();
  return connection;
}
function conectar(){
  var mysql = require('mysql');
  var connection = mysql.createConnection({
     host: 'localhost',
     user: 'root',
     password: 'eSpinoza.1',
     database: 'mayu',
     port: 3306
  });
  connection.connect(function(error){
     if(error){
        throw error;
     }else{
        console.log('Conexion correcta.');
     }
  });
  return connection;
}
function runquery(query,data){
  var connection = conectar();
  var query = connection.query(query,data, function(error, result){
     if(error){
        throw error;
     }else{
        console.log(result);
     }
   });
  connection.end();
}

module.exports.insert = function(query,data) {
  runquery(query,data);
}
module.exports.delete = function(query,data) {
  runquery(query,data);
}
module.exports.update = function(query,data) {
  runquery(query,data);
}
